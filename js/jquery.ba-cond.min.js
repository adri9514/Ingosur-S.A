
/*! jquery.ba-cond.js — helper para cadenas jQuery (.cond) */
(function ($) {
  'use strict';
  if ($.fn.cond) return; // ya existe, no lo redefinimos

  $.fn.cond = function () {
    var undef, args = arguments, i = 0, test, fn, out;
    while (out === undef && i < args.length) {
      test = args[i++]; fn = args[i++];
      test = $.isFunction(test) ? test.call(this) : test;
      out = !fn ? test : (test ? fn.call(this, test) : undef);
    }
    return out !== undef ? out : this; // permite encadenar
  };
})(jQuery);
